```
[인터넷]
    ↓
[WAF / DDoS 방어]          ← 외부 보안
    ↓
[회사 네트워크]
    ↓
[NAC]                      ← 네트워크 입구  # 네트워크에 접속 O X
    ↓
    ├── 💻 직원 PC
    ├── 🖥️ 서버
    └── 📱 스마트폰
         ↓
    [EDR / 패치관리]        ← 기기 안  # 실시간 감시 (악성코드 탐지) / 최신 버전으로 업데이트
         ↓
    [ACL / RBAC]           ← 파일/시스템 앞  # 파일 접근 권한
         ↓
    [DLP]                  ← 데이터 나가는 길목  # 중요 파일 유출 금지
         ↓
[이상행동 탐지]             ← 전체를 위에서 감시
```

### 📌 DRM
**파일 자체** 에 보안을 거는 방식

