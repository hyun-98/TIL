1. EC2 + Nginx 띄우기

1. EC2 인스턴스 생성

Private EC2 인스턴스 생성
1. 퍼블릭 IP 없음
2. Private Subnet에 있음



2. 보안 그룹 설정 (HTTP 허용)
3. 서버 접속 (SSH)
4. Nginx 설치

Nginx :
웹 서버 소프트웨어이자, 동시에 여러 가지 역할을 하는 네트워크 서버 프로그램이야. 쉽게 말하면 웹사이트를 사용자에게 보여주고, 요청을 처리하는 역할

```
nginx 설치
sudo dnf update -y
sudo dnf install nginx -y
```

```
Nginx 실행
sudo systemctl start nginx
sudo systemctl enable nginx
```

5. 웹페이지 확인

6. (중요) 서버 자동 시작 설정
